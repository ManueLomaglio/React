function AlertButton({ eventHandler }) {
  return <button onClick={eventHandler}>Vuoi sapere che ore sono?</button>;
}
export default AlertButton;
//che succede se implemento la funzione in un file a parte ?